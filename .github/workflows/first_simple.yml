name: Shell Commands

on: [push, pull]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello Arief"
      - name: multline script
        run: |
          node -v
          npm -v
          